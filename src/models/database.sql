CREATE TABLE Users (
    UserID INT IDENTITY(1,1) PRIMARY KEY,
    UserName VARCHAR(100),
    Email VARCHAR(100),
    Password VARCHAR(100),
    PhoneNumber VARCHAR(20)
    Juice VARCHAR(50),
    Coffee VARCHAR(50),
    Sandwich VARCHAR(50)
);
